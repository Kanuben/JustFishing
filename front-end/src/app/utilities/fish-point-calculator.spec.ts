import { FishPointCalculator } from './fish-point-calculator';

describe('FishPointCalculator', () => {
  it('should create an instance', () => {
    expect(new FishPointCalculator()).toBeTruthy();
  });
});
